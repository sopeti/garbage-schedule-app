{
  "name": "garbage-schedule-pipeline",
  "version": "1.0.0",
  "description": "Data gathering pipeline — fetches and normalizes Japanese municipal garbage schedules",
  "type": "module",
  "scripts": {
    "fetch": "tsx src/run.ts fetch",
    "parse": "tsx src/run.ts parse",
    "validate": "tsx src/run.ts validate",
    "run-all": "tsx src/run.ts all"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.55.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "~5.9.2"
  }
}
